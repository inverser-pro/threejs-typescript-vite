interface Settings{
    model1:      string,
    dracoPath:   string,
    canvasClass: string,
}

const settings: Settings = {
  model1:      '/models/mazda-rx-7.glb',
  dracoPath:   '/js/draco/',
  canvasClass: 'scene',
}
export default settings;